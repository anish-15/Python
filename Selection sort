## Program for Selection sort:
# Define sort function
def sort(list):
# Provide Range
    for i in range(5):
# meanposition
        pos = i
        for j in range(i, 6):
           if list[j] < list[pos]:
               pos = j
#Swapping positon of large number with smaller number
        temp = list[i]
        list[i] = list[pos]
        list[pos] = temp


list=[5,2,4,1,9,0]
sort(list)
print(list)
Output: [0,1,2,4,5,9]
